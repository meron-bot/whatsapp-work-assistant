node_modules/
dist/
coverage/
/storage/
*.log
.env
.env.local
.env.production
.DS_Store

# Secrets / credentials — NEVER commit these
googel.txt
google*.txt
*.secret
*credential*
secrets/
# Personal side-project that happens to live in this folder (has telegram.env)
שעות עבודה/
prisma/*.db
*.tsbuildinfo
dump.rdb

# Local flight watcher: config.json holds the SerpApi key; state/report are runtime
tools/flight-watch/config.json
tools/flight-watch/state.json
tools/flight-watch/report.html
